language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ssharaths/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ssharaths/docker-react npm run test
